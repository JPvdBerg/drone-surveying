# Static assets

Files here are copied as-is to the site root (under the `/drone-surveying/`
base path) at build time.

- `hero-drone.jpg` — hero banner image, referenced by
  [`src/components/Hero.tsx`](../src/components/Hero.tsx). Recommended: a
  landscape JPG ~1600px wide, compressed to keep it under ~300 KB.
